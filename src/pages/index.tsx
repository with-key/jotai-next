import React from "react";
import Modal from "../components/modal";

const Home = () => {
  return (
    <>
      <Modal.Root>
        <Modal.Close></Modal.Close>
      </Modal.Root>
    </>
  );
};

export default Home;
